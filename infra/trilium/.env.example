# TriliumNext Configuration
# Copy this file to .env and adjust as needed

# Port for TriliumNext web UI (must match PORT_REGISTRY.md)
TRILIUM_PORT=3011

# Bind address (use 127.0.0.1, not localhost, to avoid IPv6 issues)
TRILIUM_BIND=127.0.0.1

# Base URL for API calls
TRILIUM_BASE_URL=http://127.0.0.1:${TRILIUM_PORT}
